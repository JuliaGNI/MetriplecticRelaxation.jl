# macOS file system data
.DS_Store

# Files generated by invoking Julia with --code-coverage
*.jl.cov
*.jl.*.cov

# Files generated by invoking Julia with --track-allocation
*.jl.mem
*.jl.*.mem

# Build artifacts for creating documentation generated by the Documenter package
docs/build/
docs/site/

# The two output directories, root-anchored on purpose. An extension pattern alone (`*.jld2`,
# `*.pdf`) leaves whatever else a run drops behind untracked but visible, and it does not stop a
# driver from being written into an output directory. `runs/` holds the data a run produced,
# `results/` the rendered figures and reports; both are regenerable from `scripts/`.
/runs
/results

# File generated by Pkg, the package manager, based on a corresponding Project.toml
# It records a fixed state of all packages used by the project. As such, it should not be
# committed for packages, but should be committed for applications that require a static
# environment.
Manifest.toml
