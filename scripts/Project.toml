[deps]
CairoMakie = "13f3f980-e62b-5c42-98c6-ff1f3baf88f0"
FFTW = "7a1cc6ca-52ef-59f5-83cd-3a7055c09341"
LinearAlgebra = "37e2e46d-f89d-539d-b4ee-838fcccc9c8e"
MetriplecticRelaxation = "86325dbd-fdbc-4a64-9787-8114e6b727c0"
PoissonBrackets = "2a9ffa85-4e0d-473f-b05e-278d80d8d306"
Printf = "de0858da-6303-5e67-8744-51eddeeeb8d7"
Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"
SimpleSolvers = "36b790f5-6434-4fbe-b711-1f64a1e2f6a2"
SimpleSplines = "ac480f6c-9a94-4d53-a283-9db69f109154"
SparseArrays = "2f01184e-e22b-5df5-ae63-d93ebab69eaf"

# Repeated from the root `Project.toml` rather than inherited: a `[sources]` table is honoured
# only for the *active* project, so every nested environment needs its own copy.
#
# The two unregistered packages are urls for the reason the root file gives — a relative sibling
# path does not exist on a CI runner. `MetriplecticRelaxation` stays a path because it is this
# repository, so `..` is inside the checkout and resolves wherever the checkout is.
[sources]
MetriplecticRelaxation = {path = ".."}
PoissonBrackets = {rev = "main", url = "https://github.com/JuliaGNI/PoissonBrackets.jl"}
SimpleSplines = {rev = "main", url = "https://github.com/JuliaDEC/SimpleSplines.jl"}
